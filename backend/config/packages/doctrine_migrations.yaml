doctrine_migrations:
    migrations_paths:
        'Kanso\Core\Migrations': '%kanso.core_dir%/migrations'
    enable_profiler: false
    # Hand-written only: never `doctrine:migrations:diff` or `doctrine:schema:update`.
